MAINTAINERCLEANFILES = Makefile.in

TOPDIR=.
export TOPDIR

if TIMING
  TIMINGDIR =
#  TIMINGDIR = timing
else
  TIMINGDIR =
endif

if TESTING
  PIOTESTDIR = testpio
else
  PIOTESTDIR =
endif

SUBDIRS = $(TIMINGDIR) pio $(PIOTESTDIR)
