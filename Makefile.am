MAINTAINERCLEANFILES = Makefile.in


if BUILD_TIMING
  TIMINGDIR = timing
else
  TIMINGDIR =
endif

if BUILD_PIOCPP
  PIOCPPDIR = piocpp
else
  PIOCPPDIR =
endif

SUBDIRS = pio $(TIMINGDIR) $(PIOCPPDIR)
